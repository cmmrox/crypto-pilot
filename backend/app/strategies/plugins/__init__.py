"""Built-in, reviewed CryptoPilot strategy plugins.

Modules are discovered automatically. Files prefixed with an underscore are
ignored, so adding a compatible strategy does not require editing a registry.
"""
